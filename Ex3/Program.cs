class program
{
    static void Main(string[] args)
    {
        int a = 10;
        int b = 38;
        int c = (31 - 5 * a) / b;
        Console.WriteLine(c);
        // Ответ: с = 0.5, но так, как тип переменной int, ответом и будет только целое число, по этому ответ 0.
    }
}
